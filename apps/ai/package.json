{
  "name": "@zyra/ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload --app-dir src",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000 --app-dir src",
    "typecheck": "mypy src || echo 'mypy not installed'",
    "lint": "echo skip",
    "clean": "rm -rf __pycache__ .pytest_cache dist"
  }
}
